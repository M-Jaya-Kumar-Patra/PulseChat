{
  "name": "realtime-chat-app",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack real-time chat app with React, Express, and Socket.io.",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c green,cyan \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "lint": "npm run lint -w client",
    "smoke:socket": "node scripts/smoke-socket.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
